datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

model Url {
  id           Int    @id @default(autoincrement())
  hash         String @unique
  original_url String
  hits         Int    @default(1)
}
